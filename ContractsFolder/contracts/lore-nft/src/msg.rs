// In contracts/agent-nft/src/msg.rs

// This file is intentionally left mostly empty because cw721-base
// provides all the message types we need (InstantiateMsg, ExecuteMsg, QueryMsg).
// We only need to define our custom InstantiateMsg if we want to add to it,
// but for this simple case, the default is fine.